language: R
cache: packages

addons:
  apt:
    packages:
      - optipng

script:
  # Install inconsolata
  - sudo apt-get install fonts-inconsolata
  - sudo fc-cache -fv
  # Build book
  - make clean
  - make
